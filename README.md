# 📌 Тестовое задание: Senior Java Developer (Spring Boot, Netflix DGS, PostgreSQL)
Оригинальное описание задания в origin_readme.md.
## 📚 Часть 3. (опционально, по желанию, если останется время)
* Сильно переусложнённая архитектура для подобного проекта. Конечно возможно он будет расширяться, но я этого наверняка
не знаю, поэтому больше исхожу из текущей ситуации. Возможно стоило бы сделать один модуль, а разделение слоёв (контоллер, сервис и прочее)
сделать через пакеты.
* Учитывая такие потенциально проблемные моменты как составной первичный ключ, много ко многим и прочее, то я бы лучше
бы заменить маппинг через аннотации, на написанную ручками имплементацию маппера. Это даст более прозрачный,
удобочитаемый код, а следовательно поддерживать его будет легче. При более простых объектах и связях между ними
целесообразнее использовать маппинг аннотациями.
* С учётом текущего задания manyToMany здесь только ради того чтобы он был. Для реальной работы тут стоило бы использовать oneToMany
* Нагрузочные тесты обычно выполняются внешним сервисом, а не помещаются внутрь проекта как юнит тесты.

### Что я бы сделал если бы хватило времени
* Всё же попытался бы связать таблички по `referenceId`
* Поправил бы ошибки, обнаруженные при ручном тестировании
* Добавил функциональные тесты

## Комментарии к выполненной работе и принятым решениям
1. Добавил репозиторий чтобы было откуда тянуть зависимости
2. Создал новую табличу, а также промежуточную табличку для обеспечения связи Много-ко-Многим.
   По какому полю должно происходить связывание табличек не указано, однако исходя из задания предполагаю что связывание
   должно происходить по `Vanue.referenceId`. Данное поле не является первичным ключом и проводить к нему внешний ключ
   является плохой практикой. Однако, исходя из того что это "тестовое" задание я предположил что хотели увидеть именно
   такую реализацию.
3. Добавил graphql схемы для dto-шек, сгенерил классы, добавил мапперы и репозитории. В мапперах учёл циклическую
   зависимость, возникшую из-за отношения Много-ко-Многим
4. Исправил `VenuePgDao.getVenueById()` т.к. он всегда возвращал `Optional.empty()`
5. Добавил graphql схемы для API, контроллеры.
6. Исправил аннотацию над `VenueDgsMutatuion.saveVenue()`
7. Так как у нас нет метода для создания `Venue`, то заполнил ручками в БД пару записей, чтобы протестировать API
8. Протестировал API через Postman. На написание тестов времени не хватало, а так хоть какое-то тестирование будет.
   Обнаружил следующие проблемы:
    * метод `EventDgsMutation.createEvent()` при сохранении не возвращает id созданного event-а (в БД id есть). Учитывая
      что у нас нет методов получения event-а через его id предполагаю что этот момент не сильно важен. Сохранение работает
      поэтому пошёл дальше.
    * Связывание `Event` и `Venue` произошло всё же по составному первичному ключу. Времени оставалось мало, оставил как
      есть и решил допроверить проект, ведь сама связь Много-ко-Многим уже обеспечена, а значит что хотя бы часть задания
      выполнена.
    * В методе `VenueDgsQuery.venueByReferenceId()` поле `events` возвращает пустой список